/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.mazayafurniture.com",
      "orkide-technology.vercel.app",
      "www.hela-tech.com",
    ],
  },
};

export default nextConfig;
